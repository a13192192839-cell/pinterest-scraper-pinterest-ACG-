用以收集自己所需要的相关图片，修改最外层 while p<= * 的 p 的值可以控制抓取数量。但是在下一轮循环时才会检查条件所以可能会下载到大于 p 的值的数量，请注意
依靠起始 pin_id，调用 Pinterest 内部接口，获取与该图片相关联的一批推荐图片，并获取对应的pin_id，供下次爬取使用
已隔离重复出现的 pin_id 和图片地址，并且设置了在不再产生新的 pin_id 时终止循环的条件
控制了请求间隔
开发过程借助 Claude 调试完成。
x-app-version、client_tracking_params 需自行抓包获取。否则可能失效
